/**
 * 콘솔을 통해서 비밀번호를 입력받는다.
 */
task initSecrets1 doLast {
    java.io.Console console = System.console()

    print "\nType password : "
    char[] password = console.readPassword()

    ext.password = password
}

task build4Production1 {
    dependsOn initSecrets1

    dependsOn build
    build.mustRunAfter initSecrets1

    doLast {
        def password = initSecrets1.password
        // TODO: Do something
    }
}
