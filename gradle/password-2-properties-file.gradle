/**
 * .properties 파일을 통해서 비밀번호를 불러온다.
 */
task initSecrets2 doLast {
    Properties properties = new Properties()
    properties.load(rootProject.file('local.properties').newReader())

    ext.password = properties['password']
}

task build4Production2 {
    dependsOn initSecrets2

    dependsOn build
    build.mustRunAfter initSecrets2

    doLast {
        def password = initSecrets2.password
        // TODO: Do something
    }
}
